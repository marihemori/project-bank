import * as dotenv from 'dotenv';

// Carrega as variáveis do arquivo .env
dotenv.config();
{
    "type": "postgres",
    "host": "localhost",
    "port": 5432,
    "username": process.env.POSTGRES_USER,
    "password": "admin",
    "database": "project-bank",
    "entities": [
        "src/domain/entities/**.ts"
    ],
    "synchronize": true
}